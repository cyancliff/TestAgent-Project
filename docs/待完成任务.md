# 待完成任务

更新时间：2026-04-20

## 当前结论

项目当前最核心的矛盾已经变了：

- 不再是“主系统还缺很多页面”
- 也不再是“多模态只有接口没有链路”
- 现在真正要收口的是“全量多模态实验结果”和“在线主系统接入真实模型”

最近一次本地回归测试结果为 `69 passed`，所以当前优先级应从“修测试”转移到“做结果、做接入、做论文收口”。

## P0

### 1. 跑完全量多模态 baseline

状态：进行中

- [ ] 等待 `scripts/run_full_multimodal_pipeline.py` 跑完整条链路
- [ ] 记录最终 `checkpoint / val_eval / test_eval`
- [ ] 归档训练参数、运行日志和关键目录
- [ ] 确认是否存在明显过拟合或数据缺失问题

备注：

- 日志目录：`reports/full_multimodal_pipeline/`
- 运行中不要关闭黑色 `python.exe` 宿主窗口
- 不要删除 `uploads/multimodal_personality/artifacts`

### 2. 用真实 checkpoint 替换在线占位分数

状态：未开始

- [ ] 修改 `app/services/multimodal_personality_service.py`
- [ ] 从 `scaffold-v1` 切换到真实模型版本号
- [ ] 在 `run` 流程中接入真实 `bundle -> checkpoint -> score`
- [ ] 保留失败回退和错误信息输出

### 3. 把多模态结果挂到主系统里

状态：未开始

- [ ] 建立多模态任务结果与测评会话的关联
- [ ] 明确落库方式或稳定的持久化方式
- [ ] 报告页新增多模态结果区块
- [ ] 历史记录页支持查看对应多模态结果

### 4. 形成论文可用的实验记录

状态：未开始

- [ ] 整理小样本 baseline 结果
- [ ] 整理全量 baseline 结果
- [ ] 记录训练配置、设备环境和数据规模
- [ ] 形成论文中的实验表格和结果解释

## P1

### 5. 做一轮最小调参实验

状态：未开始

- [ ] 试更小学习率
- [ ] 试更短训练轮数或更强 early stopping
- [ ] 视情况跑多个随机种子
- [ ] 对比 `100 / 20`、`500 / 100`、全量结果

### 6. 把多模态结果纳入报告解释

状态：未开始

- [ ] 在报告中明确展示 Big Five 五维分数
- [ ] 增加“问卷结果 vs 视频结果”的对照说明
- [ ] 给后续多智能体辩论预留多模态上下文输入

### 7. 补工程治理项

状态：未开始

- [ ] 完整切到 Alembic 主导的数据库迁移
- [ ] 评估把线程式后台任务切换到正式任务队列
- [ ] 补更多运行监控与结构化日志
- [ ] 收口线上与本地环境说明，避免文档漂移

## P2

### 8. 继续补论文级多模态能力

状态：未开始

- [ ] 评估 `bg_features` 是否需要实现
- [ ] 评估是否继续逼近论文原始结构
- [ ] 做单模态/多模态对比实验
- [ ] 讨论 Big Five 与 ATMR 维度的关系映射

### 9. 再做产品级完善

状态：未开始

- [ ] 优化多模态上传与结果查看交互
- [ ] 补充失败任务重试入口
- [ ] 做更清晰的报告展示文案
- [ ] 再决定是否继续扩展更多 AI 能力

## 已完成但要记住的里程碑

- [x] 在线 ATMR 主系统已闭环
- [x] 多模态预处理已跑通
- [x] CLIP 特征提取已跑通
- [x] wav2clip 特征提取已跑通
- [x] feature bundle 契约已落地
- [x] AGTN-MTL baseline 已能训练、评估、推理
- [x] 已完成 `20 / 5` 与 `100 / 20` 两档真实小样本验证
- [x] 已具备可恢复的全量长任务 runner

## 当前最推荐的执行顺序

1. 等全量 baseline 跑完并固化结果。
2. 把真实 checkpoint 接到在线接口。
3. 把多模态结果接入报告与历史记录。
4. 再进入论文实验和调参阶段。
