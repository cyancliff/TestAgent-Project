# 待完成任务

更新时间：2026-04-27

## 当前结论

项目当前最核心的矛盾已经变了：

- 不再是“主系统还缺很多页面”
- 也不再是“多模态只有接口没有链路”
- 也不再是“多模态结果还没进主系统”
- 现在真正要收口的是“论文实验指标补齐”“大五报告体验打磨”和“工程治理”

全量 baseline、调参 checkpoint、在线真实推理 smoke 和大五人格报告接入已经完成，所以当前优先级应从“跑通链路/做展示”转移到“做论文、磨体验、做工程收口”。

## P0

### 1. 跑完全量多模态 baseline

状态：已完成

- [x] `scripts/run_full_multimodal_pipeline.py` 已跑完整条链路
- [x] 已记录全量 baseline 的 `checkpoint / val_eval / test_eval`
- [x] 已完成一轮调参并切换到当前最优 checkpoint
- [x] 已确认旧全量 baseline 有过拟合倾向，调参版本指标更优

备注：

- 旧全量 baseline：`reports/full_multimodal_pipeline/`
- 当前在线默认 checkpoint：`reports/night_lr1e4_drop02/agtn_mtl_lr1e4_drop02.pt`
- 当前最优测试结果：`test mse=0.011543`，`test mae=0.086074`
- 不要删除 `uploads/multimodal_personality/artifacts`

### 2. 用真实 checkpoint 替换在线占位分数并完成验证

状态：已完成

- [x] `app/services/multimodal_personality_service.py` 已接入真实推理链路
- [x] 已切换到真实模型版本号 `agtn-mtl-best-lr1e4-drop02`
- [x] `run` 流程已支持 `bundle -> checkpoint -> score`
- [x] 已保留失败回退和错误信息输出
- [x] 已完成 2026-04-25 在线 smoke 验证

备注：

- smoke 归档：`reports/online_multimodal_smoke_20260425/smoke_result.json`
- 本次真实样本返回五维分数，不再是 `0.50` 占位值

### 3. 把多模态结果挂到主系统里

状态：已完成 V1

- [x] 建立独立 `BigFivePersonalityReport` 数据模型
- [x] 明确落库方式，视频结果以独立“大五人格”报告保存
- [x] 历史记录页按 `ATMR 测评` / `大五人格` 双标签展示
- [x] 大五人格报告详情页展示五维分数、雷达图、维度简析和 AI 解读
- [x] 聊天会话可同时关联 ATMR 报告和大五人格报告
- [x] 大五报告失败、fallback 或非真实结果不会作为对话依据

### 4. 形成论文可用的实验记录

状态：进行中

- [x] 整理小样本 baseline 结果
- [x] 整理全量 baseline 与当前最优调参结果
- [x] 记录训练配置、设备环境和数据规模
- [ ] 形成论文中的实验表格和结果解释

## P1

### 5. 做一轮最小调参实验

状态：已完成一轮，仍可继续扩展

- [x] 已完成 `lr1e-4 / dropout 0.2` 等候选对比
- [x] 已选用 `night_lr1e4_drop02` 作为当前在线默认模型
- [ ] 视情况跑多个随机种子
- [ ] 补论文级指标后再决定是否继续大规模调参

### 6. 把多模态结果纳入报告解释

状态：已完成 V1，后续打磨

- [x] 在独立大五人格报告中明确展示 Big Five 五维分数
- [x] 通过 Big Five RAG / PageIndex 生成 AI 详细解读
- [x] 聊天上下文并列读取 ATMR 和大五人格报告，不强行合并结论
- [ ] 继续优化大五人格解读模板、失败提示和演示文案
- [ ] 论文中补充 ATMR 与 Big Five 两条证据链的关系说明

### 7. 补工程治理项

状态：未开始

- [ ] 完整切到 Alembic 主导的数据库迁移
- [ ] 评估把线程式后台任务切换到正式任务队列
- [ ] 补更多运行监控与结构化日志
- [ ] 收口线上与本地环境说明，避免文档漂移

## P2

### 8. 继续补论文级多模态能力

状态：未开始

- [ ] 评估 `bg_features` 是否需要实现
- [ ] 评估是否继续逼近论文原始结构
- [ ] 做单模态/多模态对比实验
- [ ] 讨论 Big Five 与 ATMR 维度的关系映射

### 9. 再做产品级完善

状态：进行中

- [x] 优化多模态上传与结果查看交互，前端统一称为“大五人格”
- [x] 补充失败任务重试入口和 AI 解读重试入口
- [x] 做更清晰的报告展示文案，去掉“多模态任务”等技术词
- [ ] 根据演示反馈继续压缩大五 AI 解读长度
- [ ] 再决定是否继续扩展更多 AI 能力

## 已完成但要记住的里程碑

- [x] 在线 ATMR 主系统已闭环
- [x] 多模态预处理已跑通
- [x] CLIP 特征提取已跑通
- [x] wav2clip 特征提取已跑通
- [x] feature bundle 契约已落地
- [x] AGTN-MTL baseline 已能训练、评估、推理
- [x] 已完成 `20 / 5` 与 `100 / 20` 两档真实小样本验证
- [x] 已具备可恢复的全量长任务 runner
- [x] 已完成 `6000 / 2000 / 2000` 全量 baseline
- [x] 已切换当前最优 checkpoint 到在线推理默认配置
- [x] 已完成在线真实推理 smoke 验证
- [x] 已完成大五人格报告接入历史页、报告页和聊天上下文
- [x] 已完成 Big Five RAG / PageIndex 知识库与 AI 解读链路

## 当前最推荐的执行顺序

1. 补齐 `PCC / CCC / R²` 等论文指标和实验表格。
2. 根据演示反馈继续打磨大五人格报告和聊天关联体验。
3. 再决定是否继续实现真实 `bg_features` 与多任务损失。
4. 最后做数据库迁移、后台任务、部署说明和答辩材料收口。
