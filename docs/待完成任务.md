# 待完成任务

更新时间：2026-04-27

## 当前结论

项目当前已经进入“基本完成，最后收口”的阶段。

已经可以确认的部分：

- 在线 ATMR 主系统已经闭环，具备注册登录、答题、报告、历史记录和 AI 咨询能力。
- 大五人格视频报告已经接入主系统，能够进入历史页、报告页和聊天上下文。
- 多模态离线链路已经跑通，并且在线服务已经可以调用真实 checkpoint 返回非占位 Big Five 分数。
- Big Five RAG / PageIndex 知识库和 AI 解读链路已经落地。
- 当前后端回归测试和前端构建都已经通过，可作为演示版继续打磨。

所以现在不再是“功能还没做出来”，而是要完成三类收尾：

1. 论文实验指标和结果表格。
2. 演示体验、文案和答辩材料。
3. 数据库迁移、后台任务和部署说明等工程治理。

## P0

### 1. 跑完全量多模态 baseline

状态：已完成

- [x] `scripts/run_full_multimodal_pipeline.py` 已跑完整条链路
- [x] 已记录全量 baseline 的 `checkpoint / val_eval / test_eval`
- [x] 已完成一轮调参并切换到当前最优 checkpoint
- [x] 已确认旧全量 baseline 有过拟合倾向，调参版本指标更优

备注：

- 旧全量 baseline：`reports/full_multimodal_pipeline/`
- 当前在线默认 checkpoint：`reports/night_lr1e4_drop02/agtn_mtl_lr1e4_drop02.pt`
- 当前最优测试结果：`test mse=0.011543`，`test mae=0.086074`
- 不要删除 `uploads/multimodal_personality/artifacts`

### 2. 用真实 checkpoint 替换在线占位分数并完成验证

状态：已完成

- [x] `app/services/multimodal_personality_service.py` 已接入真实推理链路
- [x] 已切换到真实模型版本号 `agtn-mtl-best-lr1e4-drop02`
- [x] `run` 流程已支持 `bundle -> checkpoint -> score`
- [x] 已保留失败回退和错误信息输出
- [x] 已完成 2026-04-25 在线 smoke 验证

备注：

- smoke 归档：`reports/online_multimodal_smoke_20260425/smoke_result.json`
- 本次真实样本返回五维分数，不再是 `0.50` 占位值

### 3. 把多模态结果挂到主系统里

状态：已完成 V1

- [x] 建立独立 `BigFivePersonalityReport` 数据模型
- [x] 明确落库方式，视频结果以独立“大五人格”报告保存
- [x] 历史记录页按 `ATMR 测评` / `大五人格` 双标签展示
- [x] 大五人格报告详情页展示五维分数、雷达图、维度简析和 AI 解读
- [x] 聊天会话可同时关联 ATMR 报告和大五人格报告
- [x] 大五报告失败、fallback 或非真实结果不会作为对话依据

### 4. 形成论文可用的实验记录

状态：已完成 V1，后续可继续补消融

- [x] 整理小样本 baseline 结果
- [x] 整理全量 baseline 与当前最优调参结果
- [x] 记录训练配置、设备环境和数据规模
- [x] 补齐 `ACC / PCC / CCC / R²` 等论文级指标
- [x] 形成论文中的实验表格和结果解释
- [x] 写清楚当前 baseline 与原论文完整结构之间的差异

备注：

- 论文指标汇总已生成到 `reports/multimodal_experiment_metrics.md` 和 `reports/multimodal_experiment_metrics.json`。
- 当前最优模型为 `lr1e-4 / dropout 0.2`，测试集 `MAE=0.0861`、`ACC=0.9139`、`PCC=0.7062`、`CCC=0.6327`、`R²=0.4881`。
- 后续如果时间允许，可以继续补 `bg_features` 重训对照、更多随机种子和消融实验。

### 5. 做一次完整演示走查

状态：待开始

- [ ] 注册 / 登录流程走查
- [ ] ATMR 测评完整答题、阶段提交、报告生成走查
- [ ] 历史记录中 ATMR 与大五人格双分区走查
- [ ] 大五人格视频报告上传、生成、失败提示、重试入口走查
- [ ] 聊天页同时关联 ATMR 报告和大五人格报告走查
- [ ] 记录一套答辩演示用账号、样例报告和讲解顺序

备注：

- 这一步不一定需要再加新功能，重点是发现演示时最影响观感的小问题。
- 如果真实多模态推理耗时较长，可以准备一份已经生成完成的大五人格报告用于演示。

## P1

### 6. 做一轮最小调参实验

状态：已完成一轮，仍可继续扩展

- [x] 已完成 `lr1e-4 / dropout 0.2` 等候选对比
- [x] 已选用 `night_lr1e4_drop02` 作为当前在线默认模型
- [ ] 视情况跑多个随机种子
- [ ] 基于新 `bg_features` 再决定是否继续大规模调参

### 7. 把多模态结果纳入报告解释

状态：已完成 V1，后续打磨

- [x] 在独立大五人格报告中明确展示 Big Five 五维分数
- [x] 通过 Big Five RAG / PageIndex 生成 AI 详细解读
- [x] 聊天上下文并列读取 ATMR 和大五人格报告，不强行合并结论
- [ ] 继续优化大五人格解读模板、失败提示和演示文案
- [ ] 论文中补充 ATMR 与 Big Five 两条证据链的关系说明

### 8. 补交付说明和本地产物说明

状态：待开始

- [ ] 写清楚 `reports/`、checkpoint 和多模态 artifacts 属于本地产物，不会随 Git 仓库提交
- [ ] 整理需要额外打包或备份的模型权重、实验结果和 smoke 结果
- [ ] 在部署说明中明确“轻量 Web 部署”和“本地 GPU 真实多模态推理”的区别
- [ ] 给答辩或交付准备一份最小可运行检查清单

备注：

- 当前本机产物是完整的，但 `reports/`、`uploads/multimodal_personality/artifacts/` 和 checkpoint 目录被 `.gitignore` 忽略。
- 如果要换机器演示或交给别人复现，需要单独处理这些产物。

### 9. 补工程治理项

状态：未开始

- [ ] 完整切到 Alembic 主导的数据库迁移
- [ ] 评估把线程式后台任务切换到正式任务队列
- [ ] 补更多运行监控与结构化日志
- [ ] 收口线上与本地环境说明，避免文档漂移

## P2

### 10. 继续补论文级多模态能力

状态：已完成 V1，后续可扩展

- [x] 实现工程版 256 维 `bg_features`
- [x] 接入 bundle 构建脚本和全量 pipeline
- [x] 保持旧在线 checkpoint 兼容，避免未重训时强行改变推理分布
- [ ] 使用新 `bg_features` 重新生成 bundle 并重训对照模型
- [ ] 评估是否继续逼近论文原始结构
- [ ] 做单模态/多模态对比实验
- [ ] 讨论 Big Five 与 ATMR 维度的关系映射

### 11. 再做产品级完善

状态：进行中

- [x] 优化多模态上传与结果查看交互，前端统一称为“大五人格”
- [x] 补充失败任务重试入口和 AI 解读重试入口
- [x] 做更清晰的报告展示文案，去掉“多模态任务”等技术词
- [ ] 根据演示反馈继续压缩大五 AI 解读长度
- [ ] 再决定是否继续扩展更多 AI 能力

## 已完成但要记住的里程碑

- [x] 在线 ATMR 主系统已闭环
- [x] 多模态预处理已跑通
- [x] CLIP 特征提取已跑通
- [x] wav2clip 特征提取已跑通
- [x] feature bundle 契约已落地
- [x] AGTN-MTL baseline 已能训练、评估、推理
- [x] 已完成 `20 / 5` 与 `100 / 20` 两档真实小样本验证
- [x] 已具备可恢复的全量长任务 runner
- [x] 已完成 `6000 / 2000 / 2000` 全量 baseline
- [x] 已切换当前最优 checkpoint 到在线推理默认配置
- [x] 已完成在线真实推理 smoke 验证
- [x] 已完成大五人格报告接入历史页、报告页和聊天上下文
- [x] 已完成 Big Five RAG / PageIndex 知识库与 AI 解读链路

## 当前最推荐的执行顺序

1. 做一次完整演示走查，记录答辩用账号、样例报告和讲解顺序。
2. 把 `reports/multimodal_experiment_metrics.md` 中的实验表和解释整理进论文。
3. 整理本地产物交付说明，尤其是 checkpoint、`reports/` 和多模态 artifacts。
4. 根据演示反馈继续打磨大五人格报告和聊天关联体验。
5. 视时间用新 `bg_features` 重新生成 bundle、重训并补一版对照实验。
6. 最后做数据库迁移、后台任务、部署说明等工程治理收口。
