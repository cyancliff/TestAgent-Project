repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.9
    hooks:
      # 代码格式检查 + 自动修复
      - id: ruff
        args: [--fix]
        # 排除第三方子项目
        exclude: ^PageIndex/
      # 代码格式化
      - id: ruff-format
        exclude: ^PageIndex/
